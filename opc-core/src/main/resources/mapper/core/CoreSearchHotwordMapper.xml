<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.opc.core.mapper.CoreSearchHotwordMapper">

	<resultMap type="CoreSearchHotword" id="CoreSearchHotwordResult">
		<id     property="id"                column="id"                  />
		<result property="keyword"           column="keyword"             />
		<result property="sortOrder"         column="sort_order"          />
		<result property="status"            column="status"              />
		<result property="createBy"          column="create_by"          />
		<result property="createTime"        column="create_time"        />
		<result property="updateBy"          column="update_by"          />
		<result property="updateTime"        column="update_time"        />
		<result property="remark"            column="remark"             />
	</resultMap>

	<sql id="selectSearchHotwordVo">
        select id, keyword, sort_order, status,
               create_by, create_time, update_by, update_time, remark
		from core_search_hotword
    </sql>

	<select id="selectSearchHotwordList" parameterType="CoreSearchHotword" resultMap="CoreSearchHotwordResult">
		<include refid="selectSearchHotwordVo"/>
		<where>
			<if test="keyword != null and keyword != ''">
				AND keyword like concat('%', #{keyword}, '%')
			</if>
			<if test="status != null and status != ''">
				AND status = #{status}
			</if>
		</where>
		order by sort_order asc, create_time desc
	</select>

	<select id="selectSearchHotwordById" parameterType="Long" resultMap="CoreSearchHotwordResult">
		<include refid="selectSearchHotwordVo"/>
		where id = #{id}
	</select>

	<update id="updateSearchHotword" parameterType="CoreSearchHotword">
 		update core_search_hotword
 		<set>
 			<if test="keyword != null and keyword != ''">keyword = #{keyword},</if>
 			<if test="sortOrder != null">sort_order = #{sortOrder},</if>
 			<if test="status != null and status != ''">status = #{status},</if>
 			<if test="remark != null">remark = #{remark},</if>
 			<if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
 			update_time = sysdate()
 		</set>
 		where id = #{id}
	</update>

 	<insert id="insertSearchHotword" parameterType="CoreSearchHotword" useGeneratedKeys="true" keyProperty="id">
 		insert into core_search_hotword(
 			<if test="id != null and id != 0">id,</if>
 			<if test="keyword != null and keyword != ''">keyword,</if>
 			<if test="sortOrder != null">sort_order,</if>
 			<if test="status != null and status != ''">status,</if>
 			<if test="remark != null">remark,</if>
 			<if test="createBy != null and createBy != ''">create_by,</if>
 			create_time
 		)values(
 			<if test="id != null and id != 0">#{id},</if>
 			<if test="keyword != null and keyword != ''">#{keyword},</if>
 			<if test="sortOrder != null">#{sortOrder},</if>
 			<if test="status != null and status != ''">#{status},</if>
 			<if test="remark != null">#{remark},</if>
 			<if test="createBy != null and createBy != ''">#{createBy},</if>
 			sysdate()
 		)
	</insert>

	<delete id="deleteSearchHotwordById" parameterType="Long">
		delete from core_search_hotword where id = #{id}
	</delete>

	<delete id="deleteSearchHotwordByIds" parameterType="Long">
		delete from core_search_hotword where id in
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

	<update id="changeStatus" parameterType="CoreSearchHotword">
		update core_search_hotword
		set status = #{status},
		    update_time = sysdate()
		where id = #{id}
	</update>

</mapper>
