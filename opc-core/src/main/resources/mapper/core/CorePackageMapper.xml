<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.opc.core.mapper.CorePackageMapper">

	<resultMap type="CorePackage" id="CorePackageResult">
		<id     property="id"              column="id"                />
		<result property="packageName"    column="package_name"      />
		<result property="packagePrice"    column="package_price"    />
		<result property="packageType"     column="package_type"     />
		<result property="description"     column="description"       />
		<result property="status"         column="status"            />
		<result property="createBy"       column="create_by"         />
		<result property="createTime"     column="create_time"       />
		<result property="updateBy"       column="update_by"         />
		<result property="updateTime"     column="update_time"       />
		<result property="remark"         column="remark"            />
	</resultMap>

	<sql id="selectPackageVo">
        select id, package_name, package_price, package_type, description, status,
               create_by, create_time, update_by, update_time, remark
		from core_package
    </sql>

	<select id="selectPackageList" parameterType="CorePackage" resultMap="CorePackageResult">
		<include refid="selectPackageVo"/>
		<where>
			<if test="packageName != null and packageName != ''">
				AND package_name like concat('%', #{packageName}, '%')
			</if>
			<if test="packageType != null and packageType != ''">
				AND package_type = #{packageType}
			</if>
			<if test="status != null and status != ''">
				AND status = #{status}
			</if>
		</where>
		order by package_type, id
	</select>

	<select id="selectPackageById" parameterType="Long" resultMap="CorePackageResult">
		<include refid="selectPackageVo"/>
		where id = #{id}
	</select>

	<update id="updatePackage" parameterType="CorePackage">
 		update core_package
 		<set>
 			<if test="packageName != null and packageName != ''">package_name = #{packageName},</if>
 			<if test="packagePrice != null">package_price = #{packagePrice},</if>
 			<if test="packageType != null and packageType != ''">package_type = #{packageType},</if>
 			<if test="description != null">description = #{description},</if>
 			<if test="status != null and status != ''">status = #{status},</if>
 			<if test="remark != null">remark = #{remark},</if>
 			<if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
 			update_time = sysdate()
 		</set>
 		where id = #{id}
	</update>

 	<insert id="insertPackage" parameterType="CorePackage" useGeneratedKeys="true" keyProperty="id">
 		insert into core_package(
 			<if test="id != null and id != 0">id,</if>
 			<if test="packageName != null and packageName != ''">package_name,</if>
 			<if test="packagePrice != null">package_price,</if>
 			<if test="packageType != null and packageType != ''">package_type,</if>
 			<if test="description != null">description,</if>
 			<if test="status != null and status != ''">status,</if>
 			<if test="remark != null">remark,</if>
 			<if test="createBy != null and createBy != ''">create_by,</if>
 			create_time
 		)values(
 			<if test="id != null and id != 0">#{id},</if>
 			<if test="packageName != null and packageName != ''">#{packageName},</if>
 			<if test="packagePrice != null">#{packagePrice},</if>
 			<if test="packageType != null and packageType != ''">#{packageType},</if>
 			<if test="description != null">#{description},</if>
 			<if test="status != null and status != ''">#{status},</if>
 			<if test="remark != null">#{remark},</if>
 			<if test="createBy != null and createBy != ''">#{createBy},</if>
 			sysdate()
 		)
	</insert>

	<delete id="deletePackageById" parameterType="Long">
		delete from core_package where id = #{id}
	</delete>

	<delete id="deletePackageByIds" parameterType="Long">
		delete from core_package where id in
		<foreach item="id" collection="array" open="(" separator="," close=")">
 			#{id}
 		</foreach>
	</delete>

</mapper>
