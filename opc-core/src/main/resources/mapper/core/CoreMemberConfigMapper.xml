<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.opc.core.mapper.CoreMemberConfigMapper">

	<resultMap type="CoreMemberConfig" id="CoreMemberConfigResult">
		<id     property="id"              column="id"                />
		<result property="configType"     column="config_type"       />
		<result property="imageUrl"       column="image_url"         />
		<result property="articleLink"    column="article_link"      />
		<result property="richContent"   column="rich_content"      />
		<result property="status"        column="status"            />
		<result property="createBy"       column="create_by"        />
		<result property="createTime"     column="create_time"      />
		<result property="updateBy"       column="update_by"        />
		<result property="updateTime"     column="update_time"      />
		<result property="remark"         column="remark"           />
	</resultMap>

	<sql id="selectConfigVo">
        select id, config_type, image_url, article_link, rich_content, status,
               create_by, create_time, update_by, update_time, remark
		from core_member_config
    </sql>

	<select id="selectConfigList" parameterType="CoreMemberConfig" resultMap="CoreMemberConfigResult">
		<include refid="selectConfigVo"/>
		<where>
			<if test="configType != null and configType != ''">
				AND config_type = #{configType}
			</if>
			<if test="status != null and status != ''">
				AND status = #{status}
			</if>
		</where>
		order by id
	</select>

	<select id="selectConfigById" parameterType="Long" resultMap="CoreMemberConfigResult">
		<include refid="selectConfigVo"/>
		where id = #{id}
	</select>

	<select id="selectConfigByType" parameterType="String" resultMap="CoreMemberConfigResult">
		<include refid="selectConfigVo"/>
		where config_type = #{configType}
	</select>

	<update id="updateConfig" parameterType="CoreMemberConfig">
 		update core_member_config
 		<set>
 			<if test="imageUrl != null">image_url = #{imageUrl},</if>
 			<if test="articleLink != null">article_link = #{articleLink},</if>
 			<if test="richContent != null">rich_content = #{richContent},</if>
 			<if test="status != null and status != ''">status = #{status},</if>
 			<if test="remark != null">remark = #{remark},</if>
 			<if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
 			update_time = sysdate()
 		</set>
 		where id = #{id}
	</update>

 	<insert id="insertConfig" parameterType="CoreMemberConfig" useGeneratedKeys="true" keyProperty="id">
 		insert into core_member_config(
 			<if test="id != null and id != 0">id,</if>
 			<if test="configType != null and configType != ''">config_type,</if>
 			<if test="imageUrl != null">image_url,</if>
 			<if test="articleLink != null">article_link,</if>
 			<if test="richContent != null">rich_content,</if>
 			<if test="status != null and status != ''">status,</if>
 			<if test="remark != null">remark,</if>
 			<if test="createBy != null and createBy != ''">create_by,</if>
 			create_time
 		)values(
 			<if test="id != null and id != 0">#{id},</if>
 			<if test="configType != null and configType != ''">#{configType},</if>
 			<if test="imageUrl != null">#{imageUrl},</if>
 			<if test="articleLink != null">#{articleLink},</if>
 			<if test="richContent != null">#{richContent},</if>
 			<if test="status != null and status != ''">#{status},</if>
 			<if test="remark != null">#{remark},</if>
 			<if test="createBy != null and createBy != ''">#{createBy},</if>
 			sysdate()
 		)
	</insert>

</mapper>
