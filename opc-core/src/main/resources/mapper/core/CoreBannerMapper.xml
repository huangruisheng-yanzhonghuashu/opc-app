<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.opc.core.mapper.CoreBannerMapper">

	<resultMap type="CoreBanner" id="CoreBannerResult">
		<id     property="id"                column="id"                  />
		<result property="title"              column="title"               />
		<result property="imageUrl"           column="image_url"          />
		<result property="linkType"           column="link_type"          />
		<result property="linkValue"          column="link_value"         />
		<result property="sortOrder"          column="sort_order"         />
		<result property="status"             column="status"              />
		<result property="createBy"           column="create_by"          />
		<result property="createTime"         column="create_time"        />
		<result property="updateBy"            column="update_by"          />
		<result property="updateTime"         column="update_time"        />
		<result property="remark"             column="remark"             />
	</resultMap>

	<sql id="selectBannerVo">
        select id, title, image_url, link_type, link_value, sort_order, status,
               create_by, create_time, update_by, update_time, remark
		from core_banner
    </sql>

	<select id="selectBannerList" parameterType="CoreBanner" resultMap="CoreBannerResult">
		<include refid="selectBannerVo"/>
		<where>
			<if test="title != null and title != ''">
				AND title like concat('%', #{title}, '%')
			</if>
			<if test="status != null and status != ''">
				AND status = #{status}
			</if>
		</where>
		order by sort_order asc, create_time desc
	</select>

	<select id="selectBannerById" parameterType="Long" resultMap="CoreBannerResult">
		<include refid="selectBannerVo"/>
		where id = #{id}
	</select>

	<update id="updateBanner" parameterType="CoreBanner">
 		update core_banner
 		<set>
 			<if test="title != null and title != ''">title = #{title},</if>
 			<if test="imageUrl != null and imageUrl != ''">image_url = #{imageUrl},</if>
 			<if test="linkType != null and linkType != ''">link_type = #{linkType},</if>
 			<if test="linkValue != null">link_value = #{linkValue},</if>
 			<if test="sortOrder != null">sort_order = #{sortOrder},</if>
 			<if test="status != null and status != ''">status = #{status},</if>
 			<if test="remark != null">remark = #{remark},</if>
 			<if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
 			update_time = sysdate()
 		</set>
 		where id = #{id}
	</update>

 	<insert id="insertBanner" parameterType="CoreBanner" useGeneratedKeys="true" keyProperty="id">
 		insert into core_banner(
 			<if test="id != null and id != 0">id,</if>
 			<if test="title != null and title != ''">title,</if>
 			<if test="imageUrl != null and imageUrl != ''">image_url,</if>
 			<if test="linkType != null and linkType != ''">link_type,</if>
 			<if test="linkValue != null">link_value,</if>
 			<if test="sortOrder != null">sort_order,</if>
 			<if test="status != null and status != ''">status,</if>
 			<if test="remark != null">remark,</if>
 			<if test="createBy != null and createBy != ''">create_by,</if>
 			create_time
 		)values(
 			<if test="id != null and id != 0">#{id},</if>
 			<if test="title != null and title != ''">#{title},</if>
 			<if test="imageUrl != null and imageUrl != ''">#{imageUrl},</if>
 			<if test="linkType != null and linkType != ''">#{linkType},</if>
 			<if test="linkValue != null">#{linkValue},</if>
 			<if test="sortOrder != null">#{sortOrder},</if>
 			<if test="status != null and status != ''">#{status},</if>
 			<if test="remark != null">#{remark},</if>
 			<if test="createBy != null and createBy != ''">#{createBy},</if>
 			sysdate()
 		)
	</insert>

	<delete id="deleteBannerById" parameterType="Long">
		delete from core_banner where id = #{id}
	</delete>

	<delete id="deleteBannerByIds" parameterType="Long">
		delete from core_banner where id in
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

	<update id="changeStatus" parameterType="CoreBanner">
		update core_banner
		set status = #{status},
		    update_time = sysdate()
		where id = #{id}
	</update>

</mapper>
