<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.opc.core.mapper.CoreMaterialUserActionMapper">

    <resultMap type="CoreMaterialUserAction" id="CoreMaterialUserActionResult">
        <id     property="id"           column="id"            />
        <result property="materialId"   column="material_id"   />
        <result property="userId"       column="user_id"       />
        <result property="actionType"   column="action_type"   />
        <result property="createTime"   column="create_time"   />
    </resultMap>

    <!-- 查询用户是否已执行某操作 -->
    <select id="checkUserAction" resultType="int">
        SELECT COUNT(1) FROM core_material_user_action
        WHERE material_id = #{materialId}
          AND user_id = #{userId}
          AND action_type = #{actionType}
    </select>

    <!-- 插入用户行为记录 -->
    <insert id="insertUserAction" parameterType="CoreMaterialUserAction" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO core_material_user_action(material_id, user_id, action_type, create_time)
        VALUES (#{materialId}, #{userId}, #{actionType}, NOW())
    </insert>

    <!-- 删除用户行为记录 -->
    <delete id="deleteUserAction">
        DELETE FROM core_material_user_action
        WHERE material_id = #{materialId}
          AND user_id = #{userId}
          AND action_type = #{actionType}
    </delete>

    <!-- 删除用户的所有行为记录 -->
    <delete id="deleteUserAllActions">
        DELETE FROM core_material_user_action
        WHERE material_id = #{materialId}
          AND user_id = #{userId}
    </delete>

</mapper>
